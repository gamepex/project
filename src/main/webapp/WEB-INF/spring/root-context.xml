<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:jdbc="http://www.springframework.org/schema/jdbc"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xsi:schemaLocation="http://www.springframework.org/schema/jdbc http://www.springframework.org/schema/jdbc/spring-jdbc-4.3.xsd
		http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">
	
	<!-- Root Context: defines shared resources visible to all other web components -->

	<!-- 데이터베이스(DBMS)와 스프링을 연결(연동) : DataSource -->
	<!-- Maria DB -->
	<!--
	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="org.mariadb.jdbc.Driver" />
		<property name="url" value="jdbc:mariadb://127.0.0.1:3306/storage" />
		<property name="username" value="dba" />
		<property name="password" value="dbapwd" />
	</bean>
	-->
	<!-- MySQL -->
	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="com.mysql.cj.jdbc.Driver" /><!-- MySQL JDBC Driver -->
		<property name="url" value="jdbc:mysql://127.0.0.1:3306/bookrental" /><!-- JDBC URL -->
		<property name="username" value="dba" />
		<property name="password" value="dbapwd" />
	</bean>
	
	<!-- SQL문을 수행 : SqlSession -->
	<!-- SqlSessionFactory에서 SQL문 매핑 정보를 설정한 후 SqlSession에 주입한다. -->
	<!-- name="configLocation"은 Mybatis에 대한 설정 파일의 경로를 지정한다. -->
	<!-- classpath:/ 는 src/main/resources 이다. -->
	<!-- name="mapperLocations"은 SQL문을 저장하는 xxxMapper.xml 파일의 경로를 지정한다. -->
	<!-- classpath:mappers/ 는 src/main/resources/mappers 이다. -->
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="configLocation" value="classpath:/mybatis-config.xml" />
		<property name="mapperLocations" value="classpath:mappers/**/*Mapper.xml" />
	</bean>
	<!-- SqlSession을 스프링에 등록한다. -->
	<bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate" destroy-method="clearCache">
		<constructor-arg name="sqlSessionFactory" ref="sqlSessionFactory"/>
	</bean>
	
	
	<!-- 패키지 내부에 있는 클래스를 IoC 컨테이너에 스프링빈으로 등록한다. -->
	<context:component-scan base-package="org.bookrental.admin.member" />
	<context:component-scan base-package="org.bookrental.admin.staff" />
	<context:component-scan base-package="org.bookrental.admin.book" />
	<context:component-scan base-package="org.bookrental.share" />
	<context:component-scan base-package="org.bookrental.util" />
	<context:component-scan base-package="org.bookrental.user.member" />
	
</beans>














