<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/mvc"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://www.springframework.org/schema/mvc https://www.springframework.org/schema/mvc/spring-mvc.xsd
		http://www.springframework.org/schema/beans https://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context https://www.springframework.org/schema/context/spring-context.xsd">

	<!-- DispatcherServlet Context: defines this servlet's request-processing infrastructure -->
	
	<!-- Enables the Spring MVC @Controller programming model -->
	<annotation-driven />

	<!-- 정적 서비스 자원(static resources: js, css, imgs 등)은 webapp/resources directory에서 관리한다. -->
	<!-- /resources/** 에서 첫번째 * 는 모든 폴더, 두번째 * 는 모든 파일 -->
	<resources mapping="/resources/**" location="/resources/" />
	
	<!-- 첨부 파일 업로드 디렉토리 매핑 설정 -->
	<resources mapping="/attach/**" location="file:///C:/upload/book/" />
	
	<!-- CKEditor 첨부 파일 업로드 디렉토리 매핑 설정 
	<resources mapping="/ckUpload/**" location="/resources/ckupload/" />
	-->

	
	<!-- Controller에서 응답 JSP 파일을 선택할 때 사용하는 경로(/WEB-INF/views directory)와 파일 확장자명(.jsp)을 설정 -->
	<beans:bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<beans:property name="prefix" value="/WEB-INF/views/" />
		<!-- jsp 파일이 저장된 경로를 설정 -->
		<beans:property name="suffix" value=".jsp" />
		<!-- 파일의 확장자명을 설정 -->
	</beans:bean>
	
	
	<!-- 첨부 파일을 업로드(multipart 사용) 하기 위해 CommonsMultipartResolver 객체를 스프링 빈으로 등록 -->
	<beans:bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<!-- 최대 업로드 가능한 바이트 크기(바이트 단위, 100mb), -1은 제한이 없음을 의미 -->
		<beans:property name="maxUploadSize" value="104857600" />
		<!-- 업로드 요청을 변환할 때 사용할 문자 인코딩 방식 -->
		<beans:property name="defaultEncoding" value="utf-8" />
	</beans:bean>
	
	
	<!-- 패키지 내부에 있는 클래스를 IoC 컨테이너에 스프링빈으로 등록한다. -->
	<context:component-scan base-package="org.bookrental.controller" />
	<context:component-scan base-package="org.bookrental.admin" />
	<context:component-scan base-package="org.bookrental.admin.member" />
	<context:component-scan base-package="org.bookrental.admin.staff" />
	<context:component-scan base-package="org.bookrental.admin.book" />
	<context:component-scan base-package="org.bookrental.util" />
	<context:component-scan base-package="org.bookrental.user" />
	<context:component-scan base-package="org.bookrental.user.member" />
	
</beans:beans>










