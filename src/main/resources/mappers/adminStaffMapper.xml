<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gamepex.mappers.adminStaff">

	<!-- 아이디 중복 확인 -->
	<select id="idCheck" parameterType="String" resultType="int">
		select count(*)
		from tbl_staff
		where staff_id = #{value}
	</select>

	<!-- 직원 등록 -->
	<insert id="register" parameterType="com.gamepex.admin.staff.AdminStaffVO">
		insert into tbl_staff
		(
		    staff_id
		    , staff_pw
		    , staff_name
		    , staff_gender
		    , staff_birth
		    , staff_phone
		    , staff_mail
		)
		values
		(
		    #{staff_id}
		    , #{staff_pw}
		    , #{staff_name}
		    , #{staff_gender}
		    , #{staff_birth}
		    , #{staff_phone}
		    , #{staff_mail}
		)
	</insert>
	
	<!-- 로그인 -->
	<select id="login" parameterType="String" resultType="com.gamepex.admin.staff.AdminStaffVO">
		select * from tbl_staff
		where staff_id = #{value}
	</select>
	
	<!-- 전체 직원 목록 -->
	<select id="getStaffList" resultType="com.gamepex.admin.staff.AdminStaffVO">
		select * from tbl_staff
	</select>

</mapper>











